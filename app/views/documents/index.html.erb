<p id="notice"><%= notice %></p>

<h1>Documents</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @documents.each do |document| %>
      <%= document_row_for document %>
    <% end %>
  </tbody>
</table>
<%= paginate @documents %>
<br>

<%= link_to fa_icon(:file_signature), new_document_path(:document => {:agent_id => current_agent}), :title => 'Upload Document' %>

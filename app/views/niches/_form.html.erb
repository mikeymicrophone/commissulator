<%= form_with(model: niche, local: true) do |form| %>
  <% if niche.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nich.errors.count, "error") %> prohibited this niche from being saved:</h2>

      <ul>
      <% niche.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :employer_id %>
    <%= form.select :employer_id, options_from_collection_for_select(Employer.all, :id, :name, niche.employer_id) %>
  </div>

  <div class="field">
    <%= form.label :industry_id %>
    <%= form.select :industry_id, options_from_collection_for_select(Industry.all, :id, :name, niche.industry_id) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
